# Workshop Spring Data JPA

Durée : 1 jour

Pour générer le PDF, il faut `pdflatex` et Pygment installé :

```
pdflatex --shell-escape spring-data.tex
pdflatex --shell-escape spring-data-avance.tex
```
